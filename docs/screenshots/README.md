# 截图存放处

提交文档里引用的截图都在这个文件夹。

| 文件名 | 内容 | 状态 |
|---|---|---|
| `01a-publisher.png` | 发布节点终端（colcon 编译 + `talker_node` 打印"已发布"） | ✅ 已插入文档 1.6 节 |
| `01b-subscriber.png` | 订阅节点终端（`listener_node` 打印"收到数据"） | ✅ 已插入文档 1.6 节 |
| `03-video.png` | 第 2.10 节参考的卷积讲解视频（播放界面） | ✅ 已插入文档 2.10 节 |
| `02-nodes-topics.png` | `ros2 node list` / `ros2 topic list` / `ros2 topic info /bootcamp_topic` 的输出 | ⬜ 待补 |

要求：

- 统一 PNG 格式；
- 终端截图里的字号要能在 PDF 里看清，截图前把终端字号调大；
- 文件名用半角 `-`，不要用中文标点。
